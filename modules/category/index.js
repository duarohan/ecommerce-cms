module.exports = {
  extend: '@apostrophecms/piece-type',
  options: {
    label: 'Category'
    // Additionally add a `pluralLabel` option if needed.
  },
  fields: {
    add: {},
    group: {}
  }
};
